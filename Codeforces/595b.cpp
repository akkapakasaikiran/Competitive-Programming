#include<iostream>
#include<vector>
#include<algorithm>
using namespace std;

int main(){
	int q; cin>>q;
	while(q--){
		int n; cin>>n;
		vector<int> p;
		int tmp;
		for(int i=0;i<n;i++){
			cin>>tmp;
			p.push_back(tmp);
		}

		vector<int> ans(n);
		vector<bool> vis(n);
		int len = 0;
		for(int i=0;i<n;i++){
			if(!vis[i]){
				ans[i]=
			}
		}
	}
}